/*---name: Array.finddescription: Advanced search against arrayrequire: Filter, Array.prototype.iterateprovides: Array.prototype.find, Array.prototype.findAll, Array.prototype.count...*/Array.implement({	// loop through array testing all items	filterIterator: function(first, filter, bind, direction, from, to){		/*		a speed improvment is possible is we just need to find the item through a number		if( typeof filter == 'number' ){			var value = array[(filter * direction == 'prev' ? -1 : 1) + (from || 0)];			if( typeof value == 'undefined' ) value = null;			return first ? value : [value];		}*/		return NS.Filter.filterIterator(			function(push){ this.iterate(push, this, direction, from, to); },			this,			first,			filter,			bind		);	},	// returns first item satifying filter or null	find: function(filter, bind, direction, from, to){		return this.filterIterator(true, filter, bind, direction, from, to);	},	// returns an array containing all items satifying filter	findAll: function(filter, bind, direction, from, to){		return this.filterIterator(false, filter, bind, direction, from, to);	},	// returns the number of items satisfying at least one arguments	count: function(){		return this.findAll(arguments).length;	}});