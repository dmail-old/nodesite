var StringTemplate = require('StringTemplate');
var DblMustacheTemplate = require('StringTemplate').extend({
	Parser: StringTemplate.Parser.extend({
		open: '{{',
		close: '}}',
		cache: {}, // need it's own cache
	})
});

module.exports = DblMustacheTemplate;