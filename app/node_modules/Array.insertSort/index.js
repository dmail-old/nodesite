// permet d'insérer item dans this en respectant l'ordre imposé par compare

var getSortIndex = require('./Array.getSortIndex');

module.exports = function(array, item, compare){
	array.splice(getSortIndex(array, item, compare), 0, item);
	return array;
};