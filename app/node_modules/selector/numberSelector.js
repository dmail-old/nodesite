var Selector = require('./selector');
var NumberSelector = Selector.extend({
	filter: function(){
		if( this.selector === 0 ){
			return this.ACCEPT;
		}
		this.selector--;
		return this.REJECT;
	}
});