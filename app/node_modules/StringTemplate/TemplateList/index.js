var StringTemplate = require('StringTemplate');

var proto = require('proto');
var TemplateList = proto.create({
	template: null,
	array: null, // on utiliseras ptet plutot un iterable
	seprator: null,

	init: function(template, array, separator){
		if( typeof template == 'string' ) template = StringTemplate.new(template);
		this.template = template;
		this.array = array;
		this.separator = separator;
	},

	compile: function(){
		
	},

	render: function(){
		
	},

	toString: function(){

	}
});

module.exports = TemplateList;