exports['match damien name on object'] = function(test, StringSelector){
	var selector = StringSelector.new('damien');

	test.equal(selector.match('dam'), false);
	test.equal(selector.match('damien'), false);
	test.equal(selector.match({name: 'damien'}), true);
	test.done();
};