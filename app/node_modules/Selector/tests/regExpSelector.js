exports['test the regexp on the supplied value'] = function(test, RegExpSelector){
	var selector = RegExpSelector.new(/[a-z]/);

	test.equal(selector.match(0), false);
	test.equal(selector.match('b'), true);
	test.done();
};