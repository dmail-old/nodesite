exports['should create a regExpSelector from a regExp argument'] = function(test, SelectorFactory){
	var RegExpSelector = SelectorFactory.prototype.prototypes.regexp;		
	var selector = SelectorFactory.new(/[a-z]/);

	test.equal(RegExpSelector.isPrototypeOf(selector), true);
};