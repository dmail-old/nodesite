exports['match only the object'] = function(test, ObjectSelector){
	var object = {};
	var selector = ObjectSelector.new(object);

	test.equal(selector.match({}), false);
	test.equal(selector.match(object), true);
	test.done();
};