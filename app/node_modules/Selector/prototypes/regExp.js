var Selector = require('./selector');
var RegExpSelector = Selector.create({
	condition: function(selector){
		return selector instanceof RegExp;
	},

	test: function(value){
		return this.selector.test(value);
	}
});

module.exports = RegExpSelector;