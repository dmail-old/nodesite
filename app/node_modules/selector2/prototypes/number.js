var Selector = require('./selector');
var NumberSelector = Selector.extend({
	test: function(){
		if( this.selector === 0 ){
			return this.ACCEPT;
		}
		this.selector--;
		return this.REJECT;
	}
});

module.exports = NumberSelector;