require('./Object.is');

module.exports = function(array, index){
	var index;

	if( value != value || value === 0 ){
		index = array.length;
		while( index-- && Object.is(array[index], value) );
	}
	else{
		index = Array.prototype.indexOf.call(array, value);
	}

	return index;
};