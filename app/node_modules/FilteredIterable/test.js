exports['FilteredIterable[\'@@iterator\']() return independant iterator'] = function(test){
	var iterable = ['a', 'b'];
	var filteredIterable = test.imports.new(iterable, function(){});
	var iteratorA = filteredIterable['@@iterator']();
	var iteratorB = filteredIterable['@@iterator']();

	iteratorA.next();
	test.equal(iteratorB.next().value, 'a');
	test.done();
};