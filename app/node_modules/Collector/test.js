exports['Collector[\'@@iterator\']() return independant iterator'] = function(test){
	var iterable = ['a', 'b'];
	var Collector = test.imports;
	var collector = Collector.new(iterable);
	var iteratorA = collector['@@iterator']();
	var iteratorB = collector['@@iterator']();

	iteratorA.next();
	test.equal(iteratorB.next().value, 'a');
};