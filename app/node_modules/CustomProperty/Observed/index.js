/*

var observedProperty = PropertyObserver.new('name');

observedProperty.install(object);
object.name = 'ok';
observedProperty.lastChange; // {type: 'add', oldValue: undefined, value: 'dam', object: object}

*/

var proto = require('proto');

var ObservedProperty = proto.create({
	name: null,
});

module.exports = ObservedProperty;