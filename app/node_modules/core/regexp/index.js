var keys = require('keys');

RegExp.implement = keys.implement.bind(keys, RegExp);
RegExp.complement = keys.complement.bind(keys, RegExp);

RegExp.BLANK = /\s+/;
RegExp.ALPHANUM = /^[a-zA-Z0-9_]*$/;

