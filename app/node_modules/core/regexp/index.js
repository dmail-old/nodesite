var pair = require('Object.pair');

RegExp.implement = pair.implement.bind(pair, RegExp);
RegExp.complement = pair.complement.bind(pair, RegExp);

RegExp.BLANK = /\s+/;
RegExp.ALPHANUM = /^[a-zA-Z0-9_]*$/;

