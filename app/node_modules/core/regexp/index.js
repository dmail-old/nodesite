var property = require('Object.property');

RegExp.implement = property.implement.bind(property, RegExp);
RegExp.complement = property.complement.bind(property, RegExp);

RegExp.BLANK = /\s+/;
RegExp.ALPHANUM = /^[a-zA-Z0-9_]*$/;

