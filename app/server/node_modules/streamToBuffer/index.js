module.exports = function(stream, fn, bind){
	var buffers = [];

	stream.on('data', function(data){
		buffers.push(data);
	});
	stream.on('end', function(){
		fn.call(bind, null, Buffer.concat(buffers));
	});
	stream.on('error', function(error){
		fn.call(bind, error);
	});
};