module.exports = {
	response: {
		startTime: null
	},

	handle: function(router){
		router.response.startTime = new Date();
		router.response.on('header', function(){
			this.setHeader(this.HEADERS.RESPONSE_TIME, new Date() - this.startTime);
		});
		router.next();
	}
};