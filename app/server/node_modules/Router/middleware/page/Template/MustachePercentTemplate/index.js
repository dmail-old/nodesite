var MustacheTemplate = require('MustacheTemplate');
var MustachePercentTemplate = MustacheTemplate.extend({
	Parser: MustacheTemplate.Parser.extend({
		open: '{%',
		close: '%}',
		cache: {}
	})
});

module.exports = MustachePercentTemplate;