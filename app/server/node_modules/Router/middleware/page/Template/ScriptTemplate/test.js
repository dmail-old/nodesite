exports['call toSource on values'] = function(test){
	var string = 'hello {%name%}';
	var tpl = test.imports.new();

	test.equal(tpl.render({name: {toSource: function(){ return 'damien'; }}}), 'hello damien');
	test.done();
};