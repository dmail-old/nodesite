require('property');

module.exports = {
	request: {
		params: null
	},

	handle: function(request, response, handler){
		request.params = Object.define({}, request.urlParams, request.bodyParams);
		handler.next();
	}
};