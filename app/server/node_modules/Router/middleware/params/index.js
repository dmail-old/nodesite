require('property');

module.exports = {
	request: {
		params: null
	},

	handle: function(router){
		router.request.params = Object.define({}, router.request.urlParams, router.request.bodyParams);
		router.next();
	}
};