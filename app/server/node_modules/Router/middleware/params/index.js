require('property');

module.exports = {
	request: {
		params: null
	},

	handle: function(router){
		router.request.params = Object.assign({}, router.request.urlParams, router.request.bodyParams);
		router.next();
	}
};