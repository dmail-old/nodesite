require('property');

module.exports = {
	request: {
		params: null
	},

	handle: function(request, response, handler){
		var params = {};

		Object.assign(params, request.urlParams);
		if( request.bodyParams ) Object.assign(params, request.bodyParams);

		request.params = params;
		handler.next();
	}
};