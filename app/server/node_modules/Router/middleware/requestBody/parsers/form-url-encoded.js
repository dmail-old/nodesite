require('promise-extra');
var QueryString = require('querystring');

module.exports = function(stream){
	return Promise.fromStream(stream).then(function(buffer){
		return QueryString.parse(buffer.toString());
	});
};