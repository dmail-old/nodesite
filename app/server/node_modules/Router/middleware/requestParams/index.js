require('Object.assign');

module.exports = {
	request: {
		params: null
	},

	handle: function(request, response, handler){
		var params = {};

		Object.assign(params, request.urlParams);
		if( request.body && typeof request.body === 'object' ) Object.assign(params, request.body);

		request.params = params;
		handler.next();
	}
};