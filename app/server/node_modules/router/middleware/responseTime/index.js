module.exports = {
	name: 'responseTime',

	handle: function(handler){
		handler.responseStartDate = new Date();

		handler.addTask(function(response){
			response.setHeader('x-response-time', new Date() - this.responseStartDate);
			return response;
		});
	}
};