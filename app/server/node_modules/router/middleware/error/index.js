var debug = require('debug');

module.exports = {
	name: 'error',

	task: function(handler, error){
		debug('error middleware throw the error');

		// on va répondre à l'erreur avec autre chose que l'error handler par défaut
		throw error;
	}
};