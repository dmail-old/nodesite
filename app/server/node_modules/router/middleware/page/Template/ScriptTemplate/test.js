exports['call toSource on values'] = function(test, ScriptTemplate){
	var string = 'hello {%name%}';
	var tpl = ScriptTemplate.create(string);
	var scope = {
		name: {
			toSource: function(){
				return 'damien';
			}
		}
	};

	test.equal(tpl.render(scope), 'hello damien');
};