exports['call toSource on values'] = function(test){
	var string = 'hello {%name%}';
	var tpl = test.imports.new(string);
	var scope = {
		name: {
			toSource: function(){
				return 'damien';
			}
		}
	};

	test.equal(tpl.render(scope), 'hello damien');
};