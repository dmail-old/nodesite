require('object/toSource');
var proto = require('proto');

var ScriptTemplateRenderer = Object.create(require('StringTemplate').prototype.Renderer, {
	stringify: {
		value: Object.toSource
	}
});

var ScriptTemplate = proto.extend(require('../MustachePercentTemplate'), {
	Renderer: ScriptTemplateRenderer
});

module.exports = ScriptTemplate;