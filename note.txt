---

Que fait-on maintenant? je sens que je sais plus trop quoi faire pour avancer

On pourrait ptet chercher à créer un example bateau comme phonegap (liste de telehpone)
Pour se rendre comptes des forces/faiblesses de mon système actuel de vues

déjà ce que j'aimerais c'est pouvoir créer des vues vite fait sans passer par
les object javascript genre

// lancons nous dans ce qui suit->

<ul data-controller="phonelist">
	<li ng-repeat="phone in phones">
		<p>{phone.name}</p>
	</li>
</ul>

function phonelist(scope){
	scope.add('phones', {
		name: 'coucou'
	});
};

ce que je veux réussir à faire aussi

var element = $('phonelist');

var view = View.new(element);
view.link(Model.new());

---

