exports['required module new method always create instance'] = function(test){
	var protoA = test.imports.create({
		init: function(){
			
		},

		clone: function(){
			return this.new();
		}
	});

	var protoB = protoA.create({
		init: function(){
			protoA.init.call(this);
		}
	});

	var instance = protoB.new();
	
	test.done();
};