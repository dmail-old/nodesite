require('Object.implementProperty');

function complementProperty(object, name, value){
	name = String(name);
	if( !(name in object) ){
		Object.implementProperty(object, name, value);
	}
}

module.exports = complementProperty;
Object.complementProperty = complementProperty;