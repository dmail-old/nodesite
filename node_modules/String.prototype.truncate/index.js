function truncate(length, motif){
	length = length || 0;
	motif = motif || '…';
	var str = String(this);
	return str.length > length ? str.slice(0, length - motif.length) + motif : str;
}

module.exports = truncate;
require('Object.polyfill')(String.prototype, 'truncate', truncate);