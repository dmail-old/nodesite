require('object-implement-property');

var exports = {
	complementProperty: function(object, name, value){
		if( !(name in object) ){
			Object.implementProperty(object, name, value);
		}
	}
};

Object.complementProperty = exports.complementProperty;
module.exports = exports;