var property = require('property');

function assign(object){
	property.forEachKeyIn(Array.prototype.slice.call(arguments, 1), property.assignProperty, object);
}

module.exports = assign;

if( !Object.assign ){
	Object.assign = assign;
}