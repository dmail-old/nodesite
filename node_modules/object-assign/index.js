var PropertyUpdater = require('PropertyUpdater');
var updater = new PropertyUpdater(function(object, key, owner){
	object[key] = owner[key];
});

module.exports = updater;
if( !Object.assign ){
	Object.assign = updater.update;
}