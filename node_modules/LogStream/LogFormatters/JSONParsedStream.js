var StringParsedStream = require('StringParsedStream');

var JSONParsedStream = StringParsedStream.create({
	revive: function(obj){
		return obj;
	},

	parse: function(string){
		return this.revive(JSON.parse(string));
	}
});

module.exports = JSONParsedStream;