exports['clone() keep suffix on stylizedTemplate'] = function(test, StylizedTemplate){
	var tpl = StylizedTemplate.create('ok');

	tpl.suffix = 'suff';
	var clone = tpl.clone();

	test.equal(clone.suffix, tpl.suffix);
	test.equal(clone.hasOwnProperty('suffix'), true);
};