exports['support circular references'] = function(test){
	var a = {};
	a.self = a;
	var b = test.imports.cloneProperties(a);

	test.equal(a.self, a);
	test.equal(b.self, b);
	test.ok(a != b);
	test.done();
};