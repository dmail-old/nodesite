function completeProperty(object, name, value){
	name = String(name);
	if( !(name in object) ){
		object[name] = value;
	}
}

module.exports = completeProperty;
Object.completeProperty = completeProperty;