var Task = require('task');

exports['chain task'] = function(test, Task){
	var task = Task.create(function(value){
		return Task.create(function(name){
			return Promise.resolve(name);
		}, value);
	}, 'foo');

	test.willBe(task.promise, 'foo');
};