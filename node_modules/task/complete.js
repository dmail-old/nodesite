var Task = require('task');

function complete(ms, value){
	var task;

	if( this.isPrototypeOf(value) ){
		task = value;
	}
	else{
		task = new Task().complete(value);
	}

	return task;
}

module.exports = complete;
Task.delay = complete;