exports['thenable called on resolve but not on reject'] = function(test, Task){
	var resolvedTask = Task.resolve('ok');

	test.willBe(Task.reject(resolvedTask), resolvedTask);
	test.willBe(Task.resolve(resolvedTask), 'ok');
};